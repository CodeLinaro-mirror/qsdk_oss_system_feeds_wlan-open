--- a/net/mac80211/debugfs_sta.c
+++ b/net/mac80211/debugfs_sta.c
@@ -1181,6 +1181,11 @@ void ieee80211_rx_h_sta_stats(struct sta
 		bw_idx = 0;
 
 	i = abs(status->signal);
+
+	if (i >= IEEE80211_NUM_SIGNAL) {
+		return;
+	}
+
 	sta->sdata->vif.rx_signal_pkt[i]++;
 
 	if (status->encoding & RX_ENC_HT) {
