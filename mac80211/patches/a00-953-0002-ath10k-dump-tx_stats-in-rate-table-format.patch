--- a/drivers/net/wireless/ath/ath10k/htt_rx.c
+++ b/drivers/net/wireless/ath/ath10k/htt_rx.c
@@ -2975,6 +2975,8 @@ ath10k_accumulate_per_peer_tx_stats(stru
 	ht_idx = txrate->mcs + (txrate->nss - 1) * 8;
 	bw = mac80211_bw_to_ath10k_bw(txrate->bw);
 	nss = txrate->nss - 1;
+	idx = mcs * 8 + 8 * 10 * nss;
+	idx += bw * 2 + gi;
 
 	idx = mcs * 8 + 8 * 10 * (nss - 1);
 	idx += bw * 2 + gi;
