--- a/drivers/net/wireless/ath/ath10k/core.c
+++ b/drivers/net/wireless/ath/ath10k/core.c
@@ -1655,7 +1655,7 @@ static int ath10k_core_init_firmware_fea
 		ar->max_num_vdevs = TARGET_NUM_VDEVS;
 		ar->htt.max_num_pending_tx = TARGET_NUM_MSDU_DESC;
 		ar->fw_stats_req_mask = WMI_STAT_PDEV | WMI_STAT_VDEV |
-			WMI_STAT_PEER;
+					WMI_STAT_PEER;
 		ar->max_spatial_stream = WMI_MAX_SPATIAL_STREAM;
 		break;
 	case ATH10K_FW_WMI_OP_VERSION_10_1:
@@ -1688,7 +1688,7 @@ static int ath10k_core_init_firmware_fea
 		ar->htt.max_num_pending_tx = TARGET_TLV_NUM_MSDU_DESC;
 		ar->wow.max_num_patterns = TARGET_TLV_NUM_WOW_PATTERNS;
 		ar->fw_stats_req_mask = WMI_STAT_PDEV | WMI_STAT_VDEV |
-			WMI_STAT_PEER;
+					WMI_STAT_PEER;
 		ar->max_spatial_stream = WMI_MAX_SPATIAL_STREAM;
 		break;
 	case ATH10K_FW_WMI_OP_VERSION_10_4:
@@ -1698,7 +1698,8 @@ static int ath10k_core_init_firmware_fea
 		ar->max_num_vdevs = TARGET_10_4_NUM_VDEVS;
 		ar->num_tids = TARGET_10_4_TGT_NUM_TIDS;
 		ar->htt.max_num_pending_tx = TARGET_10_4_NUM_MSDU_DESC;
-		ar->fw_stats_req_mask = WMI_STAT_PEER;
+		ar->fw_stats_req_mask = WMI_10_4_STAT_PEER |
+					WMI_10_4_STAT_PEER_EXTD;
 		ar->max_spatial_stream = WMI_10_4_MAX_SPATIAL_STREAM;
 		ar->qcache_active_peers = TARGET_10_4_QCACHE_ACTIVE_PEERS;
 		ar->tx_chain_mask = TARGET_10_4_TX_CHAIN_MASK;
@@ -1710,7 +1711,8 @@ static int ath10k_core_init_firmware_fea
 			ar->max_num_vdevs = TARGET_QCA40XX_NUM_VDEVS;
 			ar->num_tids = TARGET_QCA40XX_TGT_NUM_TIDS;
 			ar->htt.max_num_pending_tx = TARGET_QCA40XX_NUM_MSDU_DESC;
-			ar->fw_stats_req_mask = WMI_STAT_PEER;
+			ar->fw_stats_req_mask = WMI_10_4_STAT_PEER |
+						WMI_10_4_STAT_PEER_EXTD;
 			ar->max_spatial_stream = WMI_QCA40XX_MAX_SPATIAL_STREAM;
 			ar->qcache_active_peers = TARGET_QCA40XX_QCACHE_ACTIVE_PEERS;
 			ar->tx_chain_mask = TARGET_QCA40XX_TX_CHAIN_MASK;
@@ -1905,6 +1907,9 @@ int ath10k_core_start(struct ath10k *ar,
 		if (test_bit(WMI_SERVICE_COEX_GPIO, ar->wmi.svc_map))
 			val = WMI_FW_FEATURE_COEX_GPIO_SUPPORT;
 
+		if (test_bit(WMI_SERVICE_PEER_STATS, ar->wmi.svc_map))
+			val |= WMI_FW_FEATURE_PEER_STATS;
+
 		status = ath10k_wmi_ext_resource_config(ar,
 				ar->hw_params.platform_type, val);
 		if (status) {
--- a/drivers/net/wireless/ath/ath10k/wmi.c
+++ b/drivers/net/wireless/ath/ath10k/wmi.c
@@ -2647,6 +2647,16 @@ void ath10k_wmi_pull_peer_stats(const st
 	dst->peer_tx_rate = __le32_to_cpu(src->peer_tx_rate);
 }
 
+static void
+ath10k_wmi_10_4_pull_peer_stats(const struct wmi_10_4_peer_stats *src,
+				struct ath10k_fw_stats_peer *dst)
+{
+	ether_addr_copy(dst->peer_macaddr, src->peer_macaddr.addr);
+	dst->peer_rssi = __le32_to_cpu(src->peer_rssi);
+	dst->peer_tx_rate = __le32_to_cpu(src->peer_tx_rate);
+	dst->peer_rx_rate = __le32_to_cpu(src->peer_rx_rate);
+}
+
 static int ath10k_wmi_main_op_pull_fw_stats(struct ath10k *ar,
 					    struct sk_buff *skb,
 					    struct ath10k_fw_stats *stats)
@@ -2940,6 +2950,7 @@ static int ath10k_wmi_10_4_op_pull_fw_st
 	u32 num_pdev_ext_stats;
 	u32 num_vdev_stats;
 	u32 num_peer_stats;
+	u32 stats_id;
 	int i;
 
 	if (!skb_pull(skb, sizeof(*ev)))
@@ -2949,6 +2960,7 @@ static int ath10k_wmi_10_4_op_pull_fw_st
 	num_pdev_ext_stats = __le32_to_cpu(ev->num_pdev_ext_stats);
 	num_vdev_stats = __le32_to_cpu(ev->num_vdev_stats);
 	num_peer_stats = __le32_to_cpu(ev->num_peer_stats);
+	stats_id = __le32_to_cpu(ev->stats_id);
 
 	for (i = 0; i < num_pdev_stats; i++) {
 		const struct wmi_10_4_pdev_stats *src;
@@ -2988,22 +3000,28 @@ static int ath10k_wmi_10_4_op_pull_fw_st
 	/* fw doesn't implement vdev stats */
 
 	for (i = 0; i < num_peer_stats; i++) {
-		const struct wmi_10_4_peer_stats *src;
+		const struct wmi_10_4_peer_extd_stats *src;
 		struct ath10k_fw_stats_peer *dst;
+		int stats_len;
+		bool extd_peer_stats = !!(stats_id & WMI_10_4_STAT_PEER_EXTD);
+
+		if (extd_peer_stats)
+			stats_len = sizeof(struct wmi_10_4_peer_extd_stats);
+		else
+			stats_len = sizeof(struct wmi_10_4_peer_stats);
 
 		src = (void *)skb->data;
-		if (!skb_pull(skb, sizeof(*src)))
+		if (!skb_pull(skb, stats_len))
 			return -EPROTO;
 
 		dst = kzalloc(sizeof(*dst), GFP_ATOMIC);
 		if (!dst)
 			continue;
 
-		ether_addr_copy(dst->peer_macaddr, src->peer_macaddr.addr);
-		dst->peer_rssi = __le32_to_cpu(src->peer_rssi);
-		dst->peer_tx_rate = __le32_to_cpu(src->peer_tx_rate);
-		dst->peer_rx_rate = __le32_to_cpu(src->peer_rx_rate);
+		ath10k_wmi_10_4_pull_peer_stats(&src->common, dst);
 		/* FIXME: expose 10.4 specific values */
+		if (extd_peer_stats)
+			dst->rx_duration = src->rx_duration;
 
 		list_add_tail(&dst->list, &stats->peers);
 	}
--- a/drivers/net/wireless/ath/ath10k/wmi.h
+++ b/drivers/net/wireless/ath/ath10k/wmi.h
@@ -2680,6 +2680,7 @@ struct wmi_resource_config_10_4 {
 #define WMI_FW_FEATURE_AUX_RADIO_SPECTRAL_INTF    BIT(2)
 #define WMI_FW_FEATURE_AUX_RADIO_CHAN_LOAD_INTF   BIT(3)
 #define WMI_FW_FEATURE_BSS_CHANNEL_INFO_64        BIT(4)
+#define WMI_FW_FEATURE_PEER_STATS		  BIT(5)
 
 struct wmi_ext_resource_config_10_4_cmd {
     /**
@@ -4099,12 +4100,19 @@ struct wmi_pdev_stats_peer {
 } __packed;
 
 enum wmi_stats_id {
-	WMI_STAT_PEER = BIT(0),
-	WMI_STAT_AP = BIT(1),
-	WMI_STAT_PDEV = BIT(2),
-	WMI_STAT_VDEV = BIT(3),
-	WMI_STAT_BCNFLT = BIT(4),
-	WMI_STAT_VDEV_RATE = BIT(5),
+	WMI_STAT_PEER =		BIT(0),
+	WMI_STAT_AP = 		BIT(1),
+	WMI_STAT_PDEV = 	BIT(2),
+	WMI_STAT_VDEV = 	BIT(3),
+	WMI_STAT_BCNFLT = 	BIT(4),
+	WMI_STAT_VDEV_RATE = 	BIT(5),
+};
+
+enum wmi_10_4_stats_id {
+	WMI_10_4_STAT_PEER =		BIT(0),
+	WMI_10_4_STAT_AP = 		BIT(1),
+	WMI_10_4_STAT_INST = 		BIT(2),
+	WMI_10_4_STAT_PEER_EXTD = 	BIT(3),
 };
 
 struct wlan_inst_rssi_args {
@@ -4306,6 +4314,15 @@ struct wmi_10_4_peer_stats {
 	__le32 peer_rssi_changed;
 } __packed;
 
+struct wmi_10_4_peer_extd_stats {
+	struct wmi_10_4_peer_stats common;
+	struct wmi_mac_addr peer_macaddr;
+	__le32 inactive_time;
+	__le32 peer_chain_rssi;
+	__le32 rx_duration;
+	__le32 reserved[10];
+} __packed;
+
 struct wmi_10_2_pdev_ext_stats {
 	__le32 rx_rssi_comb;
 	__le32 rx_rssi[4];
--- a/drivers/net/wireless/ath/ath10k/debugfs_sta.c
+++ b/drivers/net/wireless/ath/ath10k/debugfs_sta.c
@@ -248,7 +248,8 @@ void ath10k_update_peer_tx_stats(struct
 }
 
 void ath10k_sta_update_rx_duration(struct ath10k *ar, struct list_head *head)
-{	struct ieee80211_sta *sta;
+{
+	struct ieee80211_sta *sta;
 	struct ath10k_fw_stats_peer *peer;
 	struct ath10k_sta *arsta;
 
