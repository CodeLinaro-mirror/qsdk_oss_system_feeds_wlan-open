--- a/net/mac80211/tx.c
+++ b/net/mac80211/tx.c
@@ -1560,6 +1560,9 @@ static bool ieee80211_queue_skb(struct ieee80211_local *local,
 	if (!txqi)
 		return false;
 
+	if (unlikely(local->in_reconfig))
+		return false;
+
 	spin_lock_bh(&fq->lock);
 	ieee80211_txq_enqueue(local, txqi, skb);
 	spin_unlock_bh(&fq->lock);
